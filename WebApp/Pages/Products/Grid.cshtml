@page
@model WebApp.Pages.Products.GridModel
@{
}
<div class="container-fluid">

    <div class="card">
        <div class="card-header">
            <h5 class="card-title">Inventario</h5>
        </div>
        <div class="card-body">

            <a class="btn btn-primary" asp-page="Edit">
                <i class='bx bx-add-to-queue'></i>
            </a>


            <div class="row">
                <div class="col-12 table-responsive">
                    <table id="GridView" class="table-striped table table-hover table table-bordered">
                        <thead>
                            <tr>
                                <th>Acción</th>
                                
                                <th>Codigo</th>
                                <th>Descripcion</th>
                                <th>Unidad</th>
                                <th>Cantidad Disponible</th>
                                <th>Precio Compra</th>
                                <th>Precio Venta</th>
                                <th>Costo Total</th>
                                
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.GridList)
                            {
                            <tr>
                                <td>
                                    <a class="btn btn-primary" asp-page="Edit" asp-route-id="@item.IdProducto">
                                        <i class='bx bx-edit-alt'></i>
                                    </a>
                                    <button type="button" class="btn btn-danger" onclick="ProductsGrid.OnclickEliminar(@item.IdProducto)">
                                        <i class='bx bxs-trash'></i>
                                    </button>
                                </td>
                                
                                <td>@item.Codigo</td>
                                <td>@item.Descripcion</td>
                                <td>@item.Unidad</td>
                                <td>@item.CantidadDisponible</td>
                                <td>@item.PrecioCompra</td>
                                <td>@item.PrecioVenta</td>
                                <td>@item.CostoTotal</td>
                                
                            </tr>

                            }
                        </tbody>
                    </table>


                </div>
            </div>
        </div>
    </div>
</div>

<script>
        var MensajeApp = "@Html.Raw(Model.Mensaje)";
</script>
<script defer src="~/App/Products/Grid.js"></script>